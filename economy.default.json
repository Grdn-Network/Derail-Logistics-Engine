{
  "_readme": "Copy this file to economy.json (same folder) to override the auto-derived economy. All fields are optional. Cargo names are the game's CargoType enum names. Amounts and caps are in whole carloads. Any station you do not list keeps its derived recipe (one of each input cargo makes one of each output cargo).",
  "stations": {
    "SM": {
      "defaultCap": 60,
      "caps": {
        "SteelRolls": 40
      },
      "recipes": [
        {
          "inputs": { "IronOre": 2, "CoalOre": 2 },
          "outputs": { "SteelRolls": 2 }
        }
      ]
    }
  }
}
